{
	"name": "node_angular_foundation",
	"description": "starter for node.js and angular",
	"version": "0.0.1",
	"private": true,
	"dependencies": {
		"express": "*",
        	"moment": "*",
		"passport": "*",
		"passport-local": "*",
		"passport-localapikey": "git+https://github.com/cholalabs/passport-localapikey.git",
		"node-uuid": "*",
		"bcrypt": "*",
		"cors": "*",
		"pg": "*",
		"sequelize": "git+https://github.com/sequelize/sequelize.git",
		"lodash": "*"
	},
	"devDependencies": {
		"mocha": "*",
		"supertest": "*",
		"chai": "*",
		"grunt": "*"
	},
	"scripts": {
		"init": "sudo service apache2 restart && node server.js -init",
		"start": "sudo service apache2 restart && node server.js"
	}
}
